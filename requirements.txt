# Core dependencies
pyrogram==2.0.106
pytgcalls==3.0.0.dev24
yt-dlp==2024.4.9
python-dotenv==1.0.0

# Railway compatible dependencies
requests==2.31.0
aiohttp==3.9.3
aiofiles==23.2.1

# Remove or comment these for Railway (optional)
 pymongo[srv]==4.6.1  # Only if you need MongoDB
# redis==5.0.1         # Only if you need Redis
